spring:
  application:
    name: lms
  datasource:
    url: jdbc:mysql://localhost:3307/ums?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    #hibernate:
    #  ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: false
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
server:
  port: 8080